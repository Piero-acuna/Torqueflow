specVersion: "v1"
serviceId: "torqueflow-service"
location: "southamerica-east1"

schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "torqueflow"
      cloudSql:
        instanceId: "torqueflow-service"

connectorDirs:
  - "./torqueflow-connector"