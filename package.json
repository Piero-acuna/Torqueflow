{
  "name": "torqueflow-react-firebase",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "TorqueFlow",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "node scripts/check-structure.mjs",
    "bootstrap:owner": "node scripts/bootstrap-owner.mjs",
    "clear:data": "node scripts/clear-workshop-data.mjs",
    "firebase:rules": "firebase deploy --only firestore:rules,firestore:indexes,storage"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.101.4",
    "firebase": "^12.17.1",
    "firebase-admin": "^13.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.2.0",
    "firebase-tools": "^15.26.0",
    "vite": "^7.0.0"
  }
}
